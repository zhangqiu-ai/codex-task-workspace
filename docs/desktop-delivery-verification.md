# 桌面交付验证 · 2026-09-13

版本：0.1.0-sidecar-preview，macOS arm64；分支 feature/dev，尚未发布 GitHub Release。

## 当前 AppKit 浮层

- 官方 `/Applications/ChatGPT.app` 未修改，严格签名验证通过，保留 OpenAI Team ID `2DC432GLL2`。
- AppKit 入口成功跟随真实 Codex 窗口；点击后 WKWebView 加载同一份现有 SQLite 看板数据；返回按钮可用。
- 78 项 Node 回归通过；2 项 Playwright 端到端测试通过，覆盖四列、项目/任务、任务归属、状态/Focus/Active、重载持久化、中英文与动效设置。
- 子服务由浮层持有的 stdin 管道管理；浮层退出后不遗留本地 Node 服务。
- Node 运行归档 SHA-256 在打包时再次校验，应用先暂存并验签后再原子发布。

下面内容记录已停用的应用副本实验，仅用于追溯。

## 本轮完成

- MIT 许可证及第三方声明。
- 38.6 MiB 本地安装归档，含 Node 和原生启动器；SHA-256 校验通过；不含官方 app、账号或用户数据库。
- 安装、更新、看板备份、程序回退、中断恢复和保留数据卸载。共享模式拒绝回退到旧隔离构建。
- 用户明确要求共享原版配置：安装后的路径已验证为原版 .codex 和 Application Support/Codex；不复制项目、历史或登录信息。
- 76 项测试通过；发布运行依赖审计为 0 已知漏洞（当时快照）。
- 实际更新后的应用 codesign --verify --deep --strict 通过。原版 ASAR 哈希不变。
- 原版运行时，实际调用新启动器返回 1，并提示先退出 Codex；未启动第二个共享数据宿主，未关闭原版。
- 旧 v6 应用已移入废纸篓并取消启动服务注册，旧数据保留。日常入口只使用受管理应用。

## 证据边界

先前独立配置候选通过真实登录、全局看板、会话往返、导入项目和创建任务。用户随后否决隔离方案，这些证据不能替代共享模式验收。

待用户退出原版后，验证共享模式首次启动、既有项目和任务历史可见、登录复用、设置往返一致，以及副本先启动时的原生 profile 锁。绝不删除锁以强行通过。

CUA 身份拒绝/启动超时未解决；未绕过签名校验。MCP、Hooks、Continue 全链路、长期运行、公证和下载 Gatekeeper 路径未验收。生产结论仍为 NO-GO。
