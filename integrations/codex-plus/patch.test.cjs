const test=require('node:test'),assert=require('node:assert/strict');const patch=require('./patch.cjs');
test('only main process loads Node isolation guard; sandbox preload only imports electron',()=>{const transforms=patch.patches[0].fileTransforms;const main=transforms.find(([p])=>p.includes('/main-'))[1]('/* original */');const preload=transforms.find(([p])=>p.endsWith('/preload.js'))[1]('/* original */');assert.match(main,/workspace-guard/);assert.doesNotMatch(preload,/workspace-guard/);assert.doesNotMatch(preload,/require\(["']\.\//);assert.match(preload,/workspace:page/);});
test('HTML loader rejects unknown or duplicate mount anchors',()=>{const transform=patch.patches[0].fileTransforms.find(([p])=>p.endsWith('index.html'))[1];assert.throws(()=>transform('<html/>'));assert.throws(()=>transform('</head></head>'));assert.match(transform('<head></head>'),/workspace-page.js/);});
